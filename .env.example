PORT=3000
HOST=0.0.0.0
ADMIN_USERNAME=admin
ADMIN_PASSWORD=shark2024
SESSION_SECRET=change-this-to-a-long-random-string

# Wikimedia / Open-i 在线图库（国内网络建议配置代理）
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
WIKIMEDIA_HTTP_PROXY=http://127.0.0.1:7890
# 也可复制 data/local.env 为 .env，或通过 启动后台.bat 自动注入

# 首次启动会自动创建管理员账号（写入 data/users.json）
# 普通用户通过 register.html 注册，role=user
# 仅 role=admin 可进入 /admin/ 内容后台